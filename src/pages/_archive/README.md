# Archived Pages

This folder contains legacy pages kept for reference only.

Rules:
- Do not import files from this folder in active routes.
- If a file is needed again, move it back explicitly and document the reason.
- Remove files after two stable releases if no rollback need exists.

Current archived pages:
- _SettingsLegacy.jsx
